# Fail install if the Node version does not satisfy package.json "engines" (Node 20 only).
engine-strict=true
# Expo / Metro need a flat node_modules to resolve React Native packages in a
# pnpm workspace (Expo's monorepo guide). Hoisted = the layout npm would produce.
node-linker=hoisted
