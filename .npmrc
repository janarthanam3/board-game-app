# Fail install if the Node version does not satisfy package.json "engines" (Node 20 only).
engine-strict=true
# Keep the strict pnpm node_modules layout; nothing here needs hoisting.
shamefully-hoist=false
