import { stackGap } from "@royal-navy/shared";
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  region: { flex: 1, minHeight: 0 },
  content: { gap: stackGap.tight },
});
