{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    // Pure TypeScript over plain data: no DOM, no Node types. A platform import in src/ fails here.
    "lib": ["ES2022"],
    "types": []
  },
  "include": ["src"]
}
