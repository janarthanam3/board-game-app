{
  "name": "@royal-navy/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Types, zod schemas, currency and id helpers shared by every package. No platform code.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "@typescript-eslint/parser": "~8.18.2",
    "eslint": "~9.17.0",
    "typescript": "~5.3.3",
    "vitest": "^2.0.5"
  }
}
